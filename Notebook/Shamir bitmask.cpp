

#define isBitOn(n, i) (n >> i & 1)


f(bit_mask) {
	x = f[bit_mask & (-bit_mask)];

}



int main() {
	int n = 5;
	for (int i = 0; i < 33; ++i) {
		int a[n];
		
	}

	// input
	int bit_mask;
	n |= 1 << 5;
	n ^= 1 << 5;
	if (bit_mask >> 5 & 1) {

	}


	//output

	return 0;
}
