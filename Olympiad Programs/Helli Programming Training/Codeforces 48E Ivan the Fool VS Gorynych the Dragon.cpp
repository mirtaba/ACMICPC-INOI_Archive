#include <iostream>
#include <cstdio>
#include <algorithm>
#include <cstring>
#include <string>
#include <cctype>
#include <stack>
#include <queue>
#include <list>
#include <vector>
#include <map>
#include <sstream>
#include <cmath>
#include <bitset>
#include <utility>
#include <set>
#include <numeric>
#include <cmath>

#define pb push_back
#define mp make_pair
#define f1 first
#define f2 second
#define X first
#define Y second

using namespace std;

typedef long long LL;

typedef pair <int, int> PII;

const int Maxn = 5000 +25;
const int Mod = 1000 * 1000 * 1000 + 7;

LL h,t,R,tmp,a,b;
vector <PII> head;
vector <PII> tail;
vector <PII>[Maxn][Maxn]

int main()
{
    cin >> h >> t >> R;
    cin >> tmp;
    for(int i=0;i<tmp;i++)
    {
        cin >> a >> b;
        head.push_back(make_pair(a,b));
    }
    cin >> tmp;
    for(int i=0;i<tmp;i++)
    {
        cin >> a >> b;
        tail.push_back(make_pair(a,b));
    }




}
